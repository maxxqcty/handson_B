{% extends 'base.html.twig' %}

{% block title %}User Management{% endblock %}

{% block body %}

<div class="p-6 md:p-8 space-y-6">

    <header class="flex justify-between items-center">
        <h1 class="text-3xl font-bold text-secondary">👥 User Management</h1>

        <a href="{{ path('app_user_new') }}"
           class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-lg shadow-sm text-white bg-primary hover:bg-primary-dark transition duration-150 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary">
            <span data-feather="plus" class="w-4 h-4 mr-2"></span> Create New User
        </a>
    </header>

    <div class="bg-surface-white shadow-subtle rounded-xl overflow-hidden border border-divider">

        <table class="min-w-full text-left text-sm text-secondary divide-y divide-divider">
            <thead class="bg-background-light text-secondary text-sm uppercase tracking-wider">
                <tr>
                    <th class="px-6 py-3 font-semibold">ID</th>
                    <th class="px-6 py-3 font-semibold">Username</th>
                    <th class="px-6 py-3 font-semibold">Roles</th>
                    <th class="px-6 py-3 font-semibold">Actions</th>
                </tr>
            </thead>

            <tbody class="divide-y divide-divider/50">
            {% for user in users %}
                <tr class="hover:bg-background-light/50 transition duration-100">
                    <td class="px-6 py-4 text-text-muted">{{ user.id }}</td>
                    <td class="px-6 py-4 font-medium text-secondary">{{ user.username }}</td>
                    <td class="px-6 py-4">
                        {% for role in user.roles %}
                            <span class="px-2 py-0.5 rounded-full text-xs font-semibold tracking-wider
                                {% if role == 'ROLE_ADMIN' %}
                                    bg-primary text-white
                                {% else %}
                                    bg-secondary/10 text-secondary border border-divider/50
                                {% endif %}">
                                {{ role }}
                            </span>
                        {% endfor %}
                    </td>
                    <td class="px-6 py-4 space-x-3 whitespace-nowrap">

                        <a href="{{ path('app_user_show', {'id': user.id}) }}"
                           class="inline-flex items-center text-primary hover:text-primary-dark transition duration-150 font-medium">
                            <span data-feather="eye" class="w-4 h-4 mr-1"></span> View
                        </a>

                        <a href="{{ path('app_user_edit', {'id': user.id}) }}"
                           class="inline-flex items-center text-secondary hover:text-primary transition duration-150 font-medium">
                            <span data-feather="edit-3" class="w-4 h-4 mr-1"></span> Edit
                        </a>

                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4" class="px-6 py-10 text-center text-text-muted bg-background-light">
                        No users found.
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

</div>

{% endblock %}
