{% extends 'base.html.twig' %}

{% block title %}Edit Deceased{% endblock %}

{% block body %}
    <h1>Edit Deceased</h1>

    {{ include('deceased/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_deceased_index') }}">back to list</a>

    {{ include('deceased/_delete_form.html.twig') }}
{% endblock %}
