<form method="post" action="{{ path('app_burial_record_delete', {'id': burial_record.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ burial_record.id) }}">
    <button class="btn">Delete</button>
</form>
