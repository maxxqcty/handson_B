<form method="post" action="{{ path('app_burial_plot_delete', {'id': burial_plot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ burial_plot.id) }}">
    <button class="btn">Delete</button>
</form>
